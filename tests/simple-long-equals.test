# long options with =value
program Simple
arg --integer=5
arg --string=foo
status 0
output
This is output from parsing the command line in SIMPLE.

DoInteger called with integer: 5
DoString called with "foo"
